<fieldset class="form-group">
	<label for="${param.fieldName}">
		${param.fieldLabel}
	</label>
	<input name="${param.fieldName}" type="text" class="form-control text-input" value="${param.value}"></input>
</fieldset>